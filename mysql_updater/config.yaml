name: 🐬 MySQL Periodic Updater
slug: mysql_periodic_updater
description: >
  ⏱️ Periodically updates MySQL statistics data.
  🔌 Supports connection testing.
  🧠 Configurable interval and Home Assistant sensor name.
version: "1.3.1"
startup: application
boot: auto

arch:
  - amd64
  - aarch64

options:
  interval_seconds: 300
  test_connection: false

  mysql:
    host: db.local
    port: 3306
    user: ha_user
    password: secret
    database: ha_db

  statistics:
    sensor_name: sensor.dk_spi_z10345_energy_meter

schema:
  interval_seconds: int
  test_connection: bool

  mysql:
    host: str
    port: int
    user: str
    password: password
    database: str

  statistics:
    sensor_name: str

log_level: info
